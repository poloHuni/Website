import type React from "react"
import "@/app/globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "The Local Bar And Kitchen - Where Food Meets Vibe",
  description:
    "The Local Bar And Kitchen is your neighborhood chill spot for delicious food, amazing drinks, and unforgettable moments.",
  keywords: "restaurant, bar, kitchen, local food, dining, reservations",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
